apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    application-crd-id: kubeflow-pipelines
  name: minio-service
  namespace: kubeflow
spec:
  port:
    targetPort: http
  to:
    kind: Service
    name: minio-service
    weight: 100