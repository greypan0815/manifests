apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: tumblebug-web-app
spec:
  gateways:
  - kubeflow-gateway
  hosts:
  - '*'
  http:
  - headers:
      request:
        add:
          x-forwarded-prefix: /tb
    match: # 修正: OKD 測試發現的設定問題, 多一個 `-` 導致 service 路徑不生效
    - uri:
        prefix: /tb
    rewrite:
      uri: /  # 在 forward 前，改變 uri
    route:
    - destination:
        host: tumblebug-web-app-service.kubeflow.svc.cluster.local
        port:
          number: 80
    timeout: 300s